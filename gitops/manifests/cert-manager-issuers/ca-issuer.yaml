---
# CA ClusterIssuer — signs certificates using the homelab root CA
# Use this issuer in Certificate resources for internal service TLS
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: homelab-ca-issuer
spec:
  ca:
    secretName: homelab-root-ca-secret
