apiVersion: v1
kind: Service
metadata:
  name: routeplane
  namespace: routeplane
  labels:
    app: routeplane
spec:
  selector:
    app: routeplane
  ports:
  - port: 80
    targetPort: 8080
    name: http
  type: ClusterIP
