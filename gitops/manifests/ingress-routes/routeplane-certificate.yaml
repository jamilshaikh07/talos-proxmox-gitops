apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: routeplane-tls
  namespace: routeplane
spec:
  secretName: routeplane-tls
  dnsNames:
    - routeplane.jamilshaikh.in
  issuerRef:
    name: homelab-ca-issuer
    kind: ClusterIssuer
    group: cert-manager.io
