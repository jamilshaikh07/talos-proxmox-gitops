apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: iqama-tls
  namespace: todo-app
spec:
  secretName: iqama-tls
  dnsNames:
    - iqama.space
  issuerRef:
    name: homelab-ca-issuer
    kind: ClusterIssuer
    group: cert-manager.io
