apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kagent-tls
  namespace: kagent
spec:
  secretName: kagent-tls
  dnsNames:
    - kagent.jamilshaikh.in
  issuerRef:
    name: homelab-ca-issuer
    kind: ClusterIssuer
    group: cert-manager.io
