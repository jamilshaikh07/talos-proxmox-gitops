apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mattermost-lab-tls
  namespace: mattermost
spec:
  secretName: mattermost-lab-tls
  dnsNames:
    - mattermost.lab.jamilshaikh.in
  issuerRef:
    name: homelab-ca-issuer
    kind: ClusterIssuer
    group: cert-manager.io
