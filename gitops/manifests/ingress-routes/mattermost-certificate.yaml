apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mattermost-tls
  namespace: mattermost
spec:
  secretName: mattermost-tls
  dnsNames:
    - mattermost.jamilshaikh.in
  issuerRef:
    name: homelab-ca-issuer
    kind: ClusterIssuer
    group: cert-manager.io
